# Cursor 配置

@README.md

## Cursor 专属指令

### 交互偏好

- 使用中文进行所有对话和代码注释
- 充分利用 Cursor 的 IDE 能力（代码补全、跳转到定义、重构工具等）
- 在 Agent 模式下工作时，可以主动进行代码修改
- 使用 Cursor 的内置工具（ReadLints、Glob、Grep 等）来收集信息

### 开发模式

- **Plan 模式**：当任务复杂或需要讨论方案时，先进入 Plan 模式分析
- **Agent 模式**：任务明确时直接执行，提供完整的解决方案
- 遇到需要用户决策的问题时，主动询问而非猜测

### 代码规范

- 使用 Cursor 的格式化功能保持代码风格一致
- 编辑文件前先 Read 文件内容
- 大规模重构前先创建计划与用户确认
- 遵循 README.md 中的编码规范

### 禁止事项

- 不要在未经确认的情况下执行破坏性操作（删除文件、强制覆盖等）
- 不要修改 `nginx-1.20.2/` 目录（前端已打包完成）
- 数据库变更脚本必须放入 `sql/` 目录

### 工具使用建议

- 使用 `@` 符号引用项目中的文件（`@src/...`）
- 利用 ReadLints 工具检查代码质量
- 使用 Glob/Grep 工具进行代码搜索，避免使用 shell 命令
